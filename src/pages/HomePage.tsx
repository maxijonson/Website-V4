import React from "react";
import { Header, ViewportContainer } from "../components";

export const HomePage = () => (
    <React.Fragment>
        <ViewportContainer>
            <Header />
        </ViewportContainer>
    </React.Fragment>
);
